---
- name: Installation
  hosts: localhost
  
  tasks:
  
  - name: Installation 
    apt:
      name: "{{item.a}}"
      state: "{{item.b}}"
      update_cache: "{{item.c}}"
    with_items:
      - {a: git, b: present, c: no}
      - {a: maven, b: latest, c: no}
      - {a: tomcat9, b: present, c: no}
 
  - name: maven build
    command: bash -lc "mvn clean --file *.pom && mvn compile --file *.pom && mvn package --file *.pom && cp target/java-tomcat-maven-example.war /var/lib/tomcat9/webapps"
    #command: bash -lc "mvn clean --file *.pom
  - name: Restart tomcat9
    service:
     name: tomcat9
     state: restarted
...  
